<form method="post" action="{{ path('exo_s_q_l_delete', {'id': exo_s_q_l.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exo_s_q_l.id) }}">
    <button class="btn">Delete</button>
</form>
